# Random seed generator

This function initializes a random number generator that sets the random
seed in both R and Python to ensure reproducible results from the
package.

## Usage

``` r
set_seed(seed)
```

## Arguments

- seed:

  a single integer value.

## Value

No return value.

## Details

See further examples and tutorials at
<https://mingdeyu.github.io/dgpsi-R/dev/>.

## Examples

``` r
if (FALSE) { # \dontrun{

# See dgp() for an example.
} # }
```
